# IP 클래스
 P 주소 체계는 IP주소를 효율적으로 배정하려고 클래스라는 개념을 도입했다. 클래스에는 A, B, C, D, E 다섯 종류가 있다.

# 서브넷 마스크
 서브넷 마스크는 네트워크ID와 호스트ID를 식별하기 위한 32비트의 값으로 A 클래스는 255.0.0.0, B클래스는 255.255.0.0, 
 C 클래스는 255.255.255.0이다

# 서브넷팅
 IP주소를 보다 더 효율적으로 활용할 수 있도록 네트워크를 분할하는 것을 서브넷팅, 분할된 네트워크를 서브넷이라고 한다.

# 슈퍼넷팅
 여러 개의 작은 네트워크를 하나의 커다란 네트워크로 바꾸는 작업으로 서브넷팅과 반대 개념이다.

# 학습목표
 ## TCP/IP 네트워크 계층 구조와 기능에 대한 이해를 바탕으로 설명할 수 있다.

 ## IP 주소 체계를 설명할 수 있다.

# 학습내용
네트워크 계층 구조와 역할
	네트워크 계층의 프로토콜은 IP, IGMP, ICMP, ARP, RARP 등이 있다.
	라우팅 : 다양한 경로 중 패킷의 최선의 경로를 설정한다.
	주소 변환 : 대응하는 물리 주소를 찾기 위해 논리 주소를 해석한다.
	논리 주소 지정 : 송신지와 수신지 주소를 헤더에 포함한다.
	패킷 순서 제어 : 도착하는 패킷의 순서를 바로잡아 상위 계층의 메시지를 재구성하는 데 사용한다.
IPv4 주소
	TCP/IP 프로토콜의 IP 계층에서 사용되는 식별자이다.
	IP 주소 체계는 IP 주소를 효율적으로 배정하려고 클래스라는 개념을 도입했다. 클래스에는 A, B, C, D, E 다섯 종류이다.
	서브넷팅(Subnetting)은 IP 주소를 보다 더 효율적으로 활용할 수 있도록 네트워크를 분할하는 것을 의미하며, 분할된 네트워크를 서브넷이라고 한다.
	슈퍼넷팅(Supernetting)은 여러 개의 작은 Network를 하나의 커다란 Network로 바꾸는 작업으로 서브넷팅과 반대 개념이다.